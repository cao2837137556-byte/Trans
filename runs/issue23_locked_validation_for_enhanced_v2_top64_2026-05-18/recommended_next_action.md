# Recommended Next Action

Unique first choice: `run_second_environment_or_locked_temporal_validation_before_main_method_claim`.

Because the locked leave-one-bin evidence is moderate rather than uniformly strong, do not tune topK/support/threshold further on these bins. The next clean step is either a locked temporal object not used for discovery or a second-environment validation, followed by the formal strong-baseline pack if the additional validation holds.
