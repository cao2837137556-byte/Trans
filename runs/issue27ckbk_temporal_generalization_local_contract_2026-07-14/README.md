# CKBK local contract evidence

These are deterministic local unit/contract and static dry-run artifacts.  No
model was trained, no cache/environment was created, and no HPC job was
submitted.  Both executable contract reports are `PASS`.

`real_sidecar_role_block_audit.json` additionally reads the actual frozen 1M
sidecars without training or cache mutation.  Across GLOBAL and all five held
protocols it confirms zero legal-fit overlap with hard-blocked roles and zero
legal-select overlap with report roles.
