# issue27w Decision

primary_verdict = `gotham_sample_gate_promising_needs_more_space_and_larger_sample`

Gotham passes the sample-level schema/label/timestamp availability check and has plausible device/protocol split routes. It should not proceed directly to model experiments. It should proceed to a larger Data Gate after freeing storage and building a richer manifest.
