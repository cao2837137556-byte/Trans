# issue27bw Next Action

Recommended next action:

```text
issue27bw_larger_sanity_contract_construction_or_unified_ood_margin_repair
```

Decision branch:

1. If the goal is to preserve the current strongest system, first freeze the certified parent OOD-risk channel and construct a larger sanity contract.
2. If the goal is to simplify the architecture, continue repairing the no-parent unified OOD-risk margin, but do not drop the certified parent channel until the replacement passes dev OOD <= 1%.
3. If no-parent OOD-risk remains overbudget, build mini flow-interaction evidence before adding more model heads.

Do not run a formal/full benchmark until larger sanity, mixed-stream replay, leakage checklist, and newly sealed final roles are complete.

