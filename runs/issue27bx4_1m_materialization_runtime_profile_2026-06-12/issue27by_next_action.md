# issue27by Next Action

Recommended next issue: issue27by_runtime_optimized_1m_or_slurm_materialization

Scope:
- keep the issue27bw larger contract clean;
- keep the issue27bx3 500k cache as trusted base;
- add per-file progress logging and checkpoint/quarantine rules;
- profile the `building-monitor-1` attack trace;
- retry 1M materialization locally only if the bottleneck is fixed, otherwise move high-cost PCAP extraction to Slurm;
- still no model training and no formal benchmark until a certified larger asset exists.
