# Warnings Resolution Note

## Original E3 Warning

E3 protocol audit ???? provenance ?? warning?

- `source_rich v7.2` ?????? `selected_positive_samples.csv`??? `results.csv` ????? budget/seed ? `positive_train_count`?`positive_train_first_row`?`positive_train_last_row`?
- `original100` ? `selected_positive_samples.csv` ???? count / first / last??????? selected row-id list?

? warning ?????????????????????????

## ??????

????????????? few-shot???????????????????????? support selection ??????? deterministic rule ?????? selected support row IDs?

```python
rng = np.random.default_rng(seed + int(budget) * 1009)
chosen = rng.choice(train_idx, size=int(budget), replace=False)
selected = sorted(chosen)
```

?????

- `original100`: 16-shot / 32-shot, seeds `42,43,44,45,46`
- `source_rich`: 16-shot / 32-shot, seeds `42,43,44,45,46`
- `source_rich`: 64-shot as appendix candidate only

## Validation

?? regenerated support IDs ? count / first / last ??? run ??????????????? IDs ??????

- `selected_support_ids_full.csv`
- `support_id_provenance_summary.csv`
- `support_id_provenance_validation.csv`

## Leakage Result

- candidate train pool: `2921-7042`
- attack validation: `7043-8416`
- final attack eval: `8417-9791`

?? selected support row IDs ?? candidate train pool ?????? attack validation ? final attack eval overlap?

## Resolution Status

`closed`

???????? leakage ?????????????? full row-id list ????? deterministic sampling rule ? post-hoc provenance log????? run ??????? full list??????? run ? count / first / last ?????????????????? paper-facing protocol warning?

