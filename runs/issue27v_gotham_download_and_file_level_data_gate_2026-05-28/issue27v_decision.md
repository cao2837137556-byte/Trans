# issue27v Decision

primary_verdict = `gotham_download_incomplete_resume_required`

storage_preflight_verdict = `blocked_storage_insufficient`

The Gotham file-level Data Gate cannot proceed to download/listing/extraction until the storage preflight passes. This is a Data validity gate stop, not a model result and not evidence against Gotham.
