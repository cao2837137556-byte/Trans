# Gotham Larger Sample Split Report

Most promising split: `device_disjoint_benign_drift`.

The larger sample supports candidate device-disjoint and protocol-disjoint benign drift designs, but not yet a claim-safe final benchmark. Attack support/eval can likely be separated by file, device, attack type, and time, but this must be pinned in a fuller manifest to control shortcut risks.
